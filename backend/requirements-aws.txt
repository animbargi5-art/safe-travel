# AWS Production Requirements
# Add these to your existing requirements.txt for AWS deployment

# PostgreSQL adapter
psycopg2-binary==2.9.11

# AWS SDK
boto3==1.34.0
botocore==1.34.0

# Environment management
python-dotenv==1.0.0

# Production WSGI server
gunicorn==21.2.0

# Additional production dependencies
uvloop==0.19.0  # Faster event loop for uvicorn
httptools==0.6.1  # Faster HTTP parsing

# Monitoring and logging
sentry-sdk[fastapi]==1.40.0  # Error tracking
structlog==23.2.0  # Structured logging

# Security
cryptography==41.0.8  # Enhanced security features